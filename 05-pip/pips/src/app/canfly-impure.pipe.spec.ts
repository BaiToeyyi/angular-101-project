import { CanflyImpurePipe } from './canfly-impure.pipe';

describe('CanflyImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new CanflyImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
