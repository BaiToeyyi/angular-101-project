import { SwitchColorDirective } from './switch-color.directive';

describe('SwitchColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchColorDirective();
    expect(directive).toBeTruthy();
  });
});
